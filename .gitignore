# Secrets — never commit
.env
*.env
!.env.example

# TLS certs (self-signed localhost cert/key — generated per machine)
.cert/
*.pem

# Dependencies
node_modules/

# Runtime state (regenerated from seed in store.js)
state.json

# Local tooling / OS
.tools/
.DS_Store
*.log
